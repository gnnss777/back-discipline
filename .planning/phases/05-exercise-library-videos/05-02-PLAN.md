---
phase: 05-exercise-library-videos
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - app/src/app/biblioteca/page.tsx
autonomous: true
requirements:
  - "5.4 - Update exercise cards with video"
  - "5.5 - Search and filter enhancements"

must_haves:
  truths:
    - "Exercise cards display video play button when video available"
    - "Clicking play opens modal with VideoPlayer"
    - "Search filters exercises by name"
    - "Category filter works correctly"
  artifacts:
    - path: "app/src/app/biblioteca/page.tsx"
      provides: "Updated exercise library with video modal"
      min_lines: 200
    - path: "app/src/components/VideoModal.tsx"
      provides: "Modal wrapper for VideoPlayer"
  key_links:
    - from: "app/src/app/biblioteca/page.tsx"
      to: "app/src/data/exercises.ts"
      via: "import exercises"
    - from: "app/src/app/biblioteca/page.tsx"
      to: "app/src/components/VideoPlayer.tsx"
      via: "VideoModal integration"
    - from: "app/src/app/biblioteca/page.tsx"
      to: "app/src/types/exercise.ts"
      via: "Exercise type import"
---

<objective>
Update the exercise library page with video functionality: import centralized data, add video play buttons to cards, create video modal, implement search and category filtering.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@app/src/app/biblioteca/page.tsx

**Interface: VideoModal props**
```typescript
interface VideoModalProps {
  isOpen: boolean;
  onClose: () => void;
  videoUrl: string;
  title: string;
}
```

**Interface: ExerciseCard props**
```typescript
interface ExerciseCardProps {
  exercise: Exercise;
  onPlayVideo: (exercise: Exercise) => void;
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update biblioteca page with data import and filtering</name>
  <files>app/src/app/biblioteca/page.tsx</files>
  <action>
    Update `app/src/app/biblioteca/page.tsx`:

    **Imports:**
    - Import `exercises` and `categories` from `app/src/data/exercises.ts`
    - Import `Exercise` type from `app/src/types/exercise.ts`
    - Import `Play` icon from lucide-react

    **State:**
    - Add `searchQuery` state for text search
    - Add `selectedCategory` state for category filter
    - Add `selectedExercise` state for video modal
    - Add `filteredExercises` derived from both filters

    **Filtering logic:**
    - Search filters by exercise name (case-insensitive, includes)
    - Category filter uses "Todas" to show all
    - Both filters apply together

    **Exercise Card enhancement:**
    - Show video play button overlay when exercise has videoUrl
    - Click handler calls `onPlayVideo(exercise)`
    - Hide the "VÍDEOS EM BREVE" section (no longer coming soon)

    **Remove:**
    - Delete the inline `const exercises = [...]` array
    - Delete the inline `const categories = [...]` array
  </action>
  <verify>
    <automated>npm run build --prefix app 2>&1 | Select-String -Pattern "error" -SimpleMatch</automated>
  </verify>
  <done>Biblioteca page imports from centralized data, filtering works</done>
</task>

<task type="auto">
  <name>Task 2: Create VideoModal component</name>
  <files>app/src/components/VideoModal.tsx</files>
  <action>
    Create `app/src/components/VideoModal.tsx`:

    - Full-screen overlay with dark semi-transparent background
    - Centered modal with max-width 800px
    - Close button (X) in top-right corner
    - Close on overlay click or Escape key press
    - Close on button click
    - VideoPlayer inside modal
    - Title display above video
    - Animation: fade in, scale up slightly
    - Prevent scroll on body when modal open (optional)

    Use portal: `createPortal` to render at document.body level.
  </action>
  <verify>
    <automated>Get-ChildItem -Path "app/src/components" -Filter "VideoModal.tsx" | Test-Path</automated>
  </verify>
  <done>VideoModal renders with close functionality</done>
</task>

<task type="auto">
  <name>Task 3: Integrate VideoModal into biblioteca page</name>
  <files>app/src/app/biblioteca/page.tsx</files>
  <action>
    Finalize `app/src/app/biblioteca/page.tsx`:

    **Add VideoModal import:**
    ```tsx
    import VideoModal from '@/components/VideoModal';
    ```

    **Add modal to render:**
    ```tsx
    <VideoModal
      isOpen={selectedExercise !== null}
      onClose={() => setSelectedExercise(null)}
      videoUrl={selectedExercise?.videoUrl || ''}
      title={selectedExercise?.name || ''}
    />
    ```

    **Add play button to exercise cards:**
    - Position absolute overlay on card
    - Show when exercise.videoUrl exists
    - Play circle icon with semi-transparent background
    - Hover: scale up, brighter background
  </action>
  <verify>
    <automated>npm run build --prefix app 2>&1 | Select-String -Pattern "error" -SimpleMatch</automated>
  </verify>
  <done>Video modal opens when play button clicked</done>
</task>

</tasks>

<verification>
- [ ] Exercise cards show play button for exercises with videoUrl
- [ ] Clicking play opens modal with VideoPlayer
- [ ] Modal closes on X, overlay click, or Escape
- [ ] Search filters exercises by name
- [ ] Category dropdown filters by category
- [ ] TypeScript compiles without errors
</verification>

<success_criteria>
User can browse exercises, search by name, filter by category, and watch video demos when available.
</success_criteria>

<output>
After completion, create `.planning/phases/05-exercise-library-videos/05-02-SUMMARY.md`
</output>
